*this script use in Server.

How to use.
1. Install library pySerial by open command prompt and input command "python -m pip install pyserial".
2. Install library pyodbc by open command prompt and input command "pip install pyodbc".
3. Change path in auto_config function in server_info.py to current path at project install.
4. Run server_info.py after run django.